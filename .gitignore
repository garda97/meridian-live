# Dependencies
node_modules/
__pycache__/
*.pyc

# Environment & secrets
.env
.env.*
!.env.example
.envrypt

# User config (contains RPC URL, wallet key, API keys)
user-config.json

# GMGN fee-source config (contains API key)
gmgn-config.json

# Stray Claude Code worktrees
.claude/worktrees/

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
smart-wallets.json
pool-memory.json
token-blacklist.json
strategy-library.json
decision-log.json
hivemind-cache.json
signal-weights.json

# Telegram screenshots (saved locally for Hermes vision)
uploads/*.jpg
uploads/*.png
uploads/*.webp
notes/telegram_image_pending.json

# Logs
logs/
monitor.log
*.log

# Pause flags / local runtime
.telegram_alerts_paused

# Stray config backups
user-config.json.orig
user-config.json.rej

# Local swap experiments
swap_env/

# OS
.DS_Store

# Dev context
context.md
CONTEXT.md
dev-blocklist.json
docs/

# Discord listener queue
discord-signals.json

# GIMI monitor output (scripts/gimi_monitor.py)
notes/gimi-challenges/
notes/gimi-signals.json

filter-autotune-state.json
venv_yt/

# Playwright MCP browser logs
.playwright-mcp/

# Claude Code runtime lock
.claude/scheduled_tasks.lock

# Scratch debug scripts and ad-hoc file backups from agent editing sessions
debug_*.js
*.bak.*
# Added by code-review-graph
.code-review-graph/

# ── Extra secret / runtime hardening (public repo) ───────────────────────────
# Env backups & room bots
.room-telegram.env
*.env.bak*
.env.bak*

# User-config backups (often contain RPC/LLM keys)
user-config.json.bak*
user-config.json.bak-*

# Local wallet playbooks / copytrade / metlex runtime
wallet-playbook.json
copytrade-state.json
metlex-signals.json
metlex-state.json
sol-regime-snapshots.json

# Health / recovery runtime
.health_snapshot.json
.postmortem_seen.json
.recovery_seen.json

# Claude local settings & MCP (may hold paths/tokens)
.claude/settings.json
.claude/settings.json.bak
.mcp.json

# Decision log backups
decision-log.json.bak
*.json.bak

# Notes local ban/state
notes/_gmgn_ban_state.json
notes/*_session.jsonl
notes/lp_outcome_analysis.json
notes/open_position_experience.jsonl
notes/position_watch_session.jsonl
notes/video-analysis/*.mp4

# Local secrets dir if any
secrets/
.secrets/
