# Live ops — 2026-07-14 (Grok session)

_Session by Grok for owner. `user-config.json` is gitignored — values below are the live server state._

## Code pushed (meridian-live)

| File | Change |
|------|--------|
| `agent.js` | `stream: false` on LLM calls (fixes 9router usage-history token logging) |
| `daemon/engine/screening-cycle.js` | SOL regime uses Jupiter `getSolPriceUsd()`; LLM shortlist **10 → 15** |
| `tools/sol-regime.js` | Outlier guard: reject >12% price jumps in 20min; prune bad snapshots |

## Live `user-config.json` (not in git)

### LLM / routing
- `llmBaseUrl`: `http://127.0.0.1:20128/v1` (9router local)
- `llmModel` / `screeningModel` / `managementModel` / `generalModel`: `Hermes-free`
- LLM API key in `.env` (9router key, not OpenRouter direct)

### Screening funnel (widened)
| Key | Value | Was |
|-----|-------|-----|
| `timeframe` | `30m` | 24h → 1h → 30m |
| `minVolume` | `20000` | 30000 |
| `maxTvl` | `2000000` | 1000000 |
| `maxMcap` | `5000000` | 3000000 (FEBU gate) |
| `minEstimatedSharePct` | `0.10` | 0.30 (FEBU share gate) |
| `discoveryPageSize` | `100` | unchanged |
| `opportunityPollEnabled` | `true` | unchanged |
| `useDiscordSignals` | `true` / `merge` | unchanged |

### Strategy tuning (earlier in session)
- `minEstimatedSharePct` filter: 0.35 → 0.30 → **0.10**
- `darwinEnabled`: **true**
- `partialTpEnabled`: true, `partialTpTriggerPct`: **4%**
- `autoStrategyMaxPumpPct1h`: **15** (was 35)
- `autoStrategySpotFeeTvlMin`: **2.0**
- `autoStrategySpotRatioBelow`: **0.65** (new)

### FEBU token
- Mint: `4ko5tSr5o3H4v1sFtjTSd9MPUW7yx5AFCpkNPoL6pump`
- Main pool: `2CVnAQYvrgTX8rmnRzWCE3Citgbo3kga3M8TeoFsUEJz`
- Already in `discord-signals.json`; pool-memory win rate 100% (4 deploys Jul 10)
- Blocked before by maxMcap 3M + share 0.30%; now eligible when metrics pass

## Infra (not in meridian repo)

### 9router (`/root/.9router`, systemd `9router.service`)
- Upgraded **0.5.12 → 0.5.30**
- Dashboard: `https://www.garda-lptrade.devs.surf` (Caddy → 127.0.0.1:20128)
- Patched chunk `8895.js`: removed `if(0===k&&0===l)return` so zero-token stream requests still log usage
- Combo **Hermes-free** (33 models, OpenRouter + opencode/gemini/groq)

### Services restarted
- `meridian-daemon`, `meridian-discord`, `9router`

## For Claude — suggested follow-ups

1. **Optional code**: expose `screeningCandidateLimit` in `user-config.json` (now hardcoded 15 in `screening-cycle.js`).
2. **Skill stale defaults**: update `meridian-lp-strategy` / docs that still say mcap 80k–3M, timeframe 24h, shortlist 10.
3. **Do not revert** SOL regime Jupiter price path without fixing Helius glitch ($120 vs ~$75).
4. **9router patch** is manual on server — will be lost on `npm i -g 9router` unless re-applied or upstream fixes stream usage logging.